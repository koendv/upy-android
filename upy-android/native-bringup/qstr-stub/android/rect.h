// qstr-scan-only stub -- see micropython_embed.mk's qstr-stub CFLAGS
// comment. Deliberately empty -- transitively #include'd by
// android/hardware_buffer.h (confirmed by reading the real NDK header
// directly, not guessed), same reasoning as that stub's own comment.
#ifndef UPY_ANDROID_QSTR_STUB_ANDROID_RECT_H
#define UPY_ANDROID_QSTR_STUB_ANDROID_RECT_H
#endif
